@model AbsenceManagementSystem.Model.ViewModels.AdminDashboard

@{
    ViewData["Title"] = "Home Page";
}

@* <div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div> *@



<!-- Small top boxes (Stat box) -->
<div class="row">
    <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-info">
            <div class="inner">
                <h3>@Model.NumberOfEmployees</h3>

                <p>No of Employees</p>
            </div>
            <div class="icon">
                @* <i class="ion ion-bag"></i> *@
                <i class="ion ion-person-add"></i>
            </div>
            @* <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a> *@
        </div>
    </div>
    <!-- ./col -->
    <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-success">
            <div class="inner">
                <h3>@Model.EmployeesOnCasualLeave<sup style="font-size: 20px"></sup></h3>

                <p>Employees on Casual Leave</p>
            </div>
            <div class="icon">
                @* <i class="ion ion-stats-bars"></i> *@
                <i class="ion ion-person-add"></i>
            </div>
            @* <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a> *@
        </div>
    </div>
    <!-- ./col -->
    <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-warning">
            <div class="inner">
                <h3>@Model.EmployeesOnSickLeave</h3>

                <p>Employees on Sick Leave</p>
            </div>
            <div class="icon">
                <i class="ion ion-person-add"></i>
            </div>
            @* <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a> *@
        </div>
    </div>
    <!-- ./col -->
    <div class="col-lg-3 col-6">
        <!-- small box -->
        <div class="small-box bg-danger">
            <div class="inner">
                <h3>@Model.PendingLeave</h3>

                <p>Pending Requests</p>
            </div>
            <div class="icon">
                <i class="ion ion-pie-graph"></i>
            </div>
            @* <a href="#" class="small-box-footer">More info <i class="fas fa-arrow-circle-right"></i></a> *@
        </div>
    </div>
    <!-- ./col -->
</div>
<!-- /.row -->
<!-- Main row -->


<div class="row">
    <div class="col-lg-6">

        <div class="card">
            <div class="card-header border-0">
                <h3 class="card-title">Employees</h3>
                <div class="card-tools">
                    <a href="#" class="btn btn-tool btn-sm">
                        <i class="fas fa-download"></i>
                    </a>
                    <a href="#" class="btn btn-tool btn-sm">
                        <i class="fas fa-bars"></i>
                    </a>
                </div>
            </div>
            <div class="card-body table-responsive p-0">
                <table class="table table-striped table-valign-middle">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Phone</th>
                            <th>Contract Type</th>
                            <th>No. of Leave</th>
                            <th>No. of Leave Taken</th>
                            <th>Leave Balance</th>
                            @* <th>Actions</th> *@
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var employee in Model.Employees)
                        {
                            <tr>
                                <td>
                                    @employee.FirstName @employee.LastName
                                </td>
                                <td>@employee.Email</td>
                                <td>
                                    @employee.PhoneNumber
                                </td>
                                <td>
                                    @employee.ContractType.ToString()
                                </td>
                                <td>
                                    @employee.TotalHolidayEntitlement
                                </td>
                                <td>
                                    @employee.NumberOfDaysTaken
                                </td>
                                <td>
                                    @employee.LeaveBalance
                                </td>
                                @* <td>
                                    <span>
                                        <a asp-controller="Employee" asp-action="GetEmployeeById" asp-route-roomTypeId="@employee.EmployeeId"><i class="fas fa-search"></i></a>
                                        <a href="#" class="text-muted">
                                            <i class="fas fa-search"></i>
                                        </a>
                                    </span>
                                </td> *@
                            </tr>
                        }

                        
                        
                    </tbody>
                </table>
            </div>
        </div>
        <!-- /.card -->
    </div>
    <!-- /.col-md-6 -->
    <div class="col-lg-6">

        <div class="card">
            <canvas id="monthlyLeaveChart" width="400" height="200"></canvas>
        </div>

    </div>

    <div class="col-lg-6">
        @* <div style="width:60%">
            @{
                var leaveTypeLabels1 = @Html.Raw(Json.Serialize(Model.PieChartLeaveDataDto.Select(x => x.LeaveType)));
                var leaveTypeCounts1 = @Html.Raw(Json.Serialize(Model.PieChartLeaveDataDto.Select(x => x.LeaveCount)));
            }

            <h4>Leave Requests by Type (Last 6 Months)</h4>
            <canvas id="leaveTypePieChart" width="400" height="200"></canvas>
        </div> *@







        <div class="card" style="width:100%">
            <div class="card-header border-0">
                <h3 class="card-title">Leave Requests by Type (Last 6 Months)</h3>
                <div class="card-tools">
                </div>
            </div>

            <div class="card-body">
                <div style="width:60%; margin:auto;">
                    @{
                        var leaveTypeLabels = @Html.Raw(Json.Serialize(Model.PieChartLeaveDataDto.Select(x => x.LeaveType)));
                        var leaveTypeCounts = @Html.Raw(Json.Serialize(Model.PieChartLeaveDataDto.Select(x => x.LeaveCount)));
                    }

                    <canvas id="leaveTypePieChart" width="400" height="200"></canvas>
                </div>
            </div>
        </div>
        
    </div>
    



    <div class="col-lg-6">
        <div class="card" style="width:100%">
            <div class="card-header border-0">
                <h3 class="card-title">Top 5 Employees likely to go on leave soon</h3>
                <div class="card-tools">
                </div>
            </div>

            <div class="card-body">
                @foreach (var employee in Model.EmployeesToGoOnLeaveSoon)
                {
                    <div class="d-flex justify-content-between align-items-center border-bottom mb-3">
                        <p class="text-success text-xl">
                            <i class="ion ion-ios-refresh-empty"></i>
                        </p>
                        <p class="d-flex flex-column text-right">
                            @* <span class="font-weight-bold">
                                <i class="ion ion-android-arrow-up text-success"></i> 12%
                            </span> *@
                            <span class="text-muted">@employee.EmployeeName</span>
                        </p>
                    </div>
                    <!-- /.d-flex -->
                }
            </div>
        </div>
    </div>
    
    </div>
    <!-- /.col-md-6 -->
</div>


<!-- /.row (main row) -->
<!-- OPTIONAL SCRIPTS -->
<script src="~/plugins/chart.js/chart.min.js"></script>

<script src="~/js/sitejs/demo.js"></script>
<!-- AdminLTE dashboard demo (This is only for demo purposes) -->
@* <script src="dist/js/pages/dashboard.js"></script> *@
<script src="~/js/sitejs/pages/dashboard3.js"></script>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
    var ctx = document.getElementById('monthlyLeaveChart').getContext('2d');

    // Use Model to get Month names and Leave counts
    var labels = @Html.Raw(Json.Serialize(Model.MonthlyLeaveData.Select(x => x.Month)));
    var leaveData = @Html.Raw(Json.Serialize(Model.MonthlyLeaveData.Select(x => x.LeaveCount)));

    var chart = new Chart(ctx, {
        type: 'bar',  // You can also use 'line' for a line chart
        data: {
            labels: labels,  // Month names
            datasets: [{
                label: 'Number of Employees on Leave',
                data: leaveData,  // Number of leave requests for each month
                backgroundColor: 'rgba(75, 192, 192, 0.2)',
                borderColor: 'rgba(75, 192, 192, 1)',
                borderWidth: 1
            }]
        },
        options: {
            scales: {
                y: {
                    beginAtZero: true
                }
            }
        }
    });
</script>


<script>
    var ctx = document.getElementById('leaveTypePieChart').getContext('2d');

    // Use ViewBag data for labels and data
    var labels = @leaveTypeLabels;
    var leaveData = @leaveTypeCounts;

    var chart = new Chart(ctx, {
        type: 'pie',  // Set chart type to 'pie'
        data: {
            labels: labels,  // Leave type names (Unpaid, CasualLeave, SickLeave, etc.)
            datasets: [{
                label: 'Number of Leaves',
                data: leaveData,  // Number of leave requests for each leave type
                backgroundColor: [
                    'rgba(255, 99, 132, 0.2)',   // Customize each color
                    'rgba(54, 162, 235, 0.2)',
                    'rgba(255, 206, 86, 0.2)',
                    'rgba(75, 192, 192, 0.2)',
                    'rgba(153, 102, 255, 0.2)',
                    'rgba(255, 159, 64, 0.2)'
                ],
                borderColor: [
                    'rgba(255, 99, 132, 1)',     // Border colors for each slice
                    'rgba(54, 162, 235, 1)',
                    'rgba(255, 206, 86, 1)',
                    'rgba(75, 192, 192, 1)',
                    'rgba(153, 102, 255, 1)',
                    'rgba(255, 159, 64, 1)'
                ],
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            plugins: {
                legend: {
                    display: true,
                    position: 'bottom',
                }
            }
        }
    });
</script>